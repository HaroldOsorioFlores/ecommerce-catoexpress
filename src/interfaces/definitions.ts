interface IService {
  name: string;
  url: string;
}

interface IProduct {
  _id: string;
  title: string;
  description: string;
  urlImage: string;
  price: number;
  place: string;
  createdAt: string;
  updatedAt: string;
}
